syntax = "proto3";

option csharp_namespace = "gRPC_v15";

package sonets;

service Sonets {
  rpc GetSonets (Request) returns (Reply);
}

message Request {
  string requestMessage = 1;
}

message Reply {
  string replyMessage = 1;
}
